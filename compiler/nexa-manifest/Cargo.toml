[package]
name = "nexa-manifest"
version = "0.0.1"
edition = "2021"
description = "Manifest Format 1: declarative project manifest parser and validator for nexa.project files"

[dependencies]
thiserror = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
