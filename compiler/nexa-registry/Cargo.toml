[package]
name = "nexa-registry"
version = "0.0.1"
edition = "2021"
description = "NEXA registry protocol client: query, fetch, and resolve packages from a registry"

[dependencies]
thiserror = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
