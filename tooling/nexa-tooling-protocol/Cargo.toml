[package]
name = "nexa-tooling-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "NEXA Tooling Protocol 1: transport-agnostic request/response envelopes and semantic query API"

[dependencies]
nexa-source = { path = "../../compiler/nexa-source" }
nexa-diagnostics = { path = "../../compiler/nexa-diagnostics" }
nexa-diagnostic-schema = { path = "../nexa-diagnostic-schema" }
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
